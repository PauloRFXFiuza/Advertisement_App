import {View} from "react-native"

type Props = {
    data: {
        imageURI: string
        title: string
        description: string
        price: string
    }
}

export function Product ({data} : Props){
    return <View></View>
}